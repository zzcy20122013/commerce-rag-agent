# 后续注意事项

## 一、当前正式技术选型

### 1. 文本大模型

- 正式技术选型：Doubao-Seed-2.0-lite
- 接口方式：火山方舟 OpenAI-compatible Chat Completions
- 配置项：

```env
LLM_PROVIDER=doubao
DOUBAO_API_KEY=
DOUBAO_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
DOUBAO_MODEL=doubao-seed-2-0-lite-260428
```

注意：如果火山方舟控制台要求使用自定义接入点，则把接入点 ID（通常以 `ep-` 开头）填到 `DOUBAO_MODEL`；如果账号已开通模型 ID 直连，也可以直接填具体模型 ID。

### 2. 文本 Embedding

- 正式技术选型：bge-m3 本地模型
- 接口文件：`backend/app/embeddings/bge_m3.py`
- 模型缓存目录：`C:\Users\zzcy2\Desktop\agent\models\huggingface`
- 配置项：

```env
EMBEDDING_PROVIDER=local
TEXT_EMBEDDING_PROVIDER=bge_m3
BGE_M3_MODEL_NAME=BAAI/bge-m3
BGE_M3_DEVICE=cpu
BGE_M3_ENABLE_REAL=true
```

### 3. 图片 Embedding

- 正式技术选型：Chinese-CLIP 本地模型
- 接口文件：`backend/app/embeddings/chinese_clip.py`
- 模型缓存目录：`C:\Users\zzcy2\Desktop\agent\models\huggingface`
- 配置项：

```env
IMAGE_EMBEDDING_PROVIDER=chinese_clip
CHINESE_CLIP_MODEL_NAME=OFA-Sys/chinese-clip-vit-base-patch16
CHINESE_CLIP_DEVICE=cpu
CHINESE_CLIP_ENABLE_REAL=true
CHINESE_CLIP_LOCAL_FILES_ONLY=true
```

### 4. 本地模型清理提醒

- bge-m3 / Chinese-CLIP 权重集中在：`C:\Users\zzcy2\Desktop\agent\models`
- 后续删除这个 `models` 目录会同时删除两个本地模型。
- 删除后源代码和项目文档仍然保留，但需要重新下载 bge-m3 / Chinese-CLIP，并重新构建文本索引和图片索引。

## 二、当前下一步提醒

当前 Android 原生端核心链路已经完成，后续不要急着先做正式路演界面，优先做：

1. Plan 08：扩充 10-20 条小规模评测集，固定推荐、筛选、对比、多轮、无严格匹配和 ResponseComposer 表达场景。
2. Plan 06：继续打磨 Android UI、滚动体验、商品卡片布局、图片上传结果和真机/模拟器兼容性。
3. Plan 05：继续用评测结果反推 Agent 导购表达、多轮约束继承、主推/备选/劝退话术。
4. 等 Agent 回答和 Android 体验稳定后，再做 Plan 07 路演界面。

## 三、Android 展示端运行提醒

当前环境已安装 Android Studio，并已创建 Pixel 模拟器。Android 端当前访问本机后端地址为：

```text
http://10.0.2.2:8000
```

运行前确认：

1. 后端已启动在 `127.0.0.1:8000`。
2. Android Studio 打开的是 `C:\Users\zzcy2\Desktop\agent\android`。
3. 模拟器网络可以访问宿主机。
4. Android 构建可运行：

```powershell
cd C:\Users\zzcy2\Desktop\agent\android
.\gradlew.bat :app:assembleDebug
```

后续继续打磨：

1. UI 视觉与正式 AI 导购产品一致性。
2. 侧边栏会话删除交互。
3. 商品卡片移动端布局。
4. 图片上传后的图搜展示。
5. 真机体验验证。

## 四、索引重建提醒

切换 Embedding 模型或向量维度后，必须重建 Chroma 索引，否则会出现维度冲突。

执行位置：

```text
C:\Users\zzcy2\Desktop\agent\backend
```

执行命令：

```powershell
python -m app.scripts.rebuild_indexes
```

## 五、路演界面后续单独制作

当前 Web Debug 前端用于开发调试和能力验证。

路演界面不要和 Debug Console 混在一起，后续单独做 Demo Showcase 页面，定位为项目路演的正式产品展示界面。

路演界面要求：

1. 不显示 trace、intent、filters、retrieved ids、raw SSE 等调试信息。
2. 重点展示类似豆包聊天体验、图片上传、商品卡片、推荐理由和反馈。
3. 视觉上更像正式 AI 导购产品，而不是开发后台。
4. 可以和 Web Debug 共用 API client、SSE parser、商品卡片数据结构和上传/反馈接口。
